proj{StudentId, StudentName, GroupId}(sigma{FacultyName = :FacultyName && FacultyId = GroupFacultyId}(Students njoin Groups njoin Faculties)) except (proj{StudentId, StudentName, GroupId}(sigma{CourseName = :CourseName}(Students njoin Marks njoin Courses)))