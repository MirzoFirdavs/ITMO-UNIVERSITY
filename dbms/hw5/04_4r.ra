proj{StudentId, StudentName, GroupId}(sigma{CourseName = :CourseName}(Students njoin Plan njoin Courses)) except (proj{StudentId, StudentName, GroupId}(sigma{CourseName = :CourseName}(Students njoin Marks njoin Courses)))