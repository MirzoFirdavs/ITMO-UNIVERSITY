proj{StudentName, CourseName}(proj{StudentId, StudentName, CourseName}(Students njoin Plan njoin Courses) except proj{StudentId, StudentName, CourseName}(sigma{mark >= 4}(Students njoin Plan njoin Marks njoin Courses)))